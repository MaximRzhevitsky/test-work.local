{{! Garvity forms Single Line Text Field component https://docs.gravityforms.com/single-line-text-field/ https://docs.gravityforms.com/validation-errors/ }}

<div id="field_{{gFormIndex}}_{{gFormFieldIndex true}}"
	 class="gfield gfield_single_line_text {{gformFieldErrorClass showError}} {{gformAddCustomClass
		 customClass}} {{gformRequiredClass required}}  field_sublabel_below field_description_below {{gformHideLabel
		 hideLabel}} gfield_visibility_visible">
	<label class="gfield_label" for="input_{{gFormIndex}}_{{gFormFieldIndex}}">
		{{labelText}}{{#with required}}<span class="gfield_required">*</span>{{/with}}
	</label>
	<div class="ginput_container ginput_container_text">
		<input
			name="input_{{gFormFieldIndex}}"
			id="input_{{gFormIndex}}_{{gFormFieldIndex}}"
			type="{{#if typeTextOrPassword}}{{typeTextOrPassword}}{{else}}text{{/if}}"
			value="{{defaultValue}}"
			class="{{fieldSize}}"
			placeholder="{{placeholderText}}"
			aria-required="{{gformIsRequired required}}"
			aria-invalid="false"
		>
	</div>
	{{#if descriptionText}}
		<div class="gfield_description">{{descriptionText}}</div>
	{{/if}}
	{{#if showError}}
		<div class="gfield_description validation_message">
			{{#if customValidationText}}
				{{customValidationText}}
			{{else}}
				This field is required.
			{{/if}}
		</div>
	{{/if}}
</div>
